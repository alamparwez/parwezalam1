name: hello-world
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  my-job:
    runs-on: self-hosted
    steps:
      - name: my-step
        run: echo "Hello World!"
      - name: command1
        run: pwd 
