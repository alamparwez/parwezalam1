name: hello-world
'on': push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"
      - name: my-commands
        run: pwd | echo "parwez" > 1.txt | ls | cat 1.txt
